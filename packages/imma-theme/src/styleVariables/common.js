const standartFont = `"Questrial", Arial, Helvetica, sans-serif`;
const titleFont = `"SpaceMono", Arial, Helvetica, sans-serif`;

const common = {
  titleFont,
  standartFont,
};

export default common;
